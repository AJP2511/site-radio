import React from "react";
import { MainContainer } from "../styles/Header.styled";

const Header = () => {
  return (
    <MainContainer>
      <div>LOGO</div>
      <div>Nome</div>
    </MainContainer>
  );
};

export default Header;
